<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="Pixelatte.UI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="using:Pixelatte.UI.Behaviors"
    xmlns:converters="using:CommunityToolkit.WinUI.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:local="using:Pixelatte.UI"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:Pixelatte.UI.Views"
    x:Name="MainWindowInstance"
    Title="Pixelatte"
    mc:Ignorable="d">

    <Grid x:Name="AppContainer">
        <interactivity:Interaction.Behaviors>
            <behaviors:InitializeOptionsBehavior
                ExtendsContentIntoTitleBar="True"
                MainWindow="{x:Bind MainWindowInstance}"
                MaximizeAtStartup="True" />
        </interactivity:Interaction.Behaviors>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar
            Title="Pixelatte"
            HorizontalContentAlignment="Center"
            IsBackButtonEnabled="{x:Bind RootFrame.CanGoBack, Mode=OneWay}"
            IsBackButtonVisible="True">
            <interactivity:Interaction.Behaviors>
                <behaviors:BackRequestedBehavior FrameControl="{x:Bind RootFrame}" Page="{Binding Page, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            </interactivity:Interaction.Behaviors>
            <TitleBar.LeftHeader>
                <ImageIcon
                    Width="20"
                    Margin="0,0,5,0"
                    Source="/Assets/icon.ico" />
            </TitleBar.LeftHeader>
            <TitleBar.Content>
                <Grid
                    MinWidth="100"
                    Margin="-77,0,0,0"
                    Padding="4"
                    VerticalAlignment="Center"
                    Background="{ThemeResource SystemControlAccentAcrylicElementAccentMediumHighBrush}"
                    CornerRadius="7"
                    Visibility="{Binding OperationTitle, Converter={StaticResource OperationTitleToVisibilityConverter}}">
                    <TextBlock HorizontalAlignment="Center" Text="{Binding OperationTitle}" />
                </Grid>
            </TitleBar.Content>
        </TitleBar>
        <Grid
            Grid.Row="1"
            Height="500"
            VerticalAlignment="Top">
            <Rectangle>
                <Rectangle.Fill>
                    <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                        <GradientStop Offset="0.0" Color="#2C5364" />
                        <GradientStop Offset="1" Color="#000" />
                    </LinearGradientBrush>
                </Rectangle.Fill>
            </Rectangle>
        </Grid>
        <Frame
            x:Name="RootFrame"
            Grid.Row="1"
            x:FieldModifier="internal"
            Background="Transparent">
            <interactivity:Interaction.Behaviors>
                <behaviors:MvvmNavigationBehavior Page="{Binding Page, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            </interactivity:Interaction.Behaviors>
        </Frame>
    </Grid>

    <!--<Grid x:Name="AppContainer">
        <Grid.Resources>
            <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
            <converters:BoolNegationConverter x:Key="BoolNegationConverter" />
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <StackPanel
            Grid.ColumnSpan="2"
            Padding="10"
            Orientation="Vertical">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Button Command="{Binding LoadImageCommand}" Style="{ThemeResource AccentButtonStyle}">Load image</Button>
                <TextBox
                    Grid.Column="1"
                    IsReadOnly="True"
                    PlaceholderText="Choose an image"
                    Text="{Binding SelectedImagePath}" />
            </Grid>
            <Expander
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Stretch"
                ExpandDirection="Down"
                IsExpanded="True"
                Visibility="{Binding ShowContent, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                <Expander.Header>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Style="{ThemeResource SubtitleTextBlockStyle}">Original</TextBlock>
                    </StackPanel>
                </Expander.Header>
                <Grid RowSpacing="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*" />
                        <ColumnDefinition Width="6*" />
                        <ColumnDefinition Width="2*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="auto" />
                    </Grid.RowDefinitions>
                    <StackPanel
                        Grid.Column="1"
                        HorizontalAlignment="Center"
                        BorderBrush="{ThemeResource AccentFillColorSecondaryBrush}"
                        BorderThickness="1"
                        CornerRadius="1">
                        <Image
                            x:Name="imgContainer"
                            MaxWidth="450"
                            Source="{Binding SelectedImage}"
                            Stretch="Uniform" />
                    </StackPanel>
                    <ItemsRepeater
                        Grid.Row="1"
                        Grid.Column="1"
                        ItemsSource="{Binding Tags, UpdateSourceTrigger=PropertyChanged}">
                        <ItemsRepeater.Layout>
                            <LinedFlowLayout ItemsJustification="Center" MinItemSpacing="5" />
                        </ItemsRepeater.Layout>
                        <ItemsRepeater.ItemTemplate>
                            <DataTemplate x:DataType="x:String">
                                <Border Background="{ThemeResource AccentAcrylicBackgroundFillColorBaseBrush}" CornerRadius="5">
                                    <TextBlock
                                        Padding="10,3"
                                        HorizontalAlignment="Center"
                                        Foreground="{ThemeResource AccentAAFillColorSecondaryBrush}"
                                        Style="{ThemeResource BodyStrongTextBlockStyle}"
                                        Text="{x:Bind}" />
                                </Border>
                            </DataTemplate>
                        </ItemsRepeater.ItemTemplate>
                    </ItemsRepeater>
                </Grid>
            </Expander>

        </StackPanel>
        <ScrollViewer
            Grid.Row="1"
            Grid.Column="1"
            IsEnabled="{Binding IsLoading, Mode=OneWay, Converter={StaticResource BoolNegationConverter}}">
            <StackPanel
                Padding="10"
                Orientation="Vertical"
                Spacing="10">
                <Grid Visibility="{Binding ShowContent, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                    <Expander
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Stretch"
                        ExpandDirection="Down">
                        <Expander.Header>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Style="{ThemeResource SubtitleTextBlockStyle}">Grayscale</TextBlock>
                                <HyperlinkButton ToolTipService.ToolTip="Open in a new window">
                                    <FontIcon Glyph="&#xE8A7;" />
                                </HyperlinkButton>
                            </StackPanel>
                        </Expander.Header>
                        <Grid RowSpacing="10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto" />
                                <RowDefinition />
                            </Grid.RowDefinitions>
                            <StackPanel Orientation="Horizontal" Spacing="10">
                                <ToggleSwitch Margin="0,0,-80,0" IsOn="{Binding ApplyGrayscale, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />
                                <CheckBox IsChecked="{Binding ShowOriginalInGrayscale, Mode=TwoWay}">Show original</CheckBox>
                            </StackPanel>
                            <StackPanel
                                Grid.Row="1"
                                HorizontalAlignment="Center"
                                BorderBrush="{ThemeResource AccentFillColorSecondaryBrush}"
                                BorderThickness="1"
                                CornerRadius="1"
                                Orientation="Horizontal"
                                Visibility="{Binding ApplyGrayscale, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                                <Image
                                    MaxWidth="{Binding ElementName=imgContainer, Path=MaxWidth}"
                                    MaxHeight="{Binding ElementName=imgContainer, Path=MaxHeight}"
                                    Source="{Binding SelectedImage}"
                                    Stretch="Uniform"
                                    Visibility="{Binding ShowOriginalInGrayscale, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                                <Border
                                    BorderBrush="{ThemeResource AccentFillColorSecondaryBrush}"
                                    BorderThickness="0.5"
                                    Visibility="{Binding ShowOriginalInGrayscale, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                                <Image
                                    MaxWidth="{Binding ElementName=imgContainer, Path=MaxWidth}"
                                    MaxHeight="{Binding ElementName=imgContainer, Path=MaxHeight}"
                                    Source="{Binding GrayscaleImage}"
                                    Stretch="Uniform" />
                            </StackPanel>
                        </Grid>
                    </Expander>
                </Grid>
                <Grid Visibility="{Binding ShowContent, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                    <Expander
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Stretch"
                        ExpandDirection="Down">
                        <Expander.Header>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Style="{ThemeResource SubtitleTextBlockStyle}">Basic pixel operations</TextBlock>
                                <HyperlinkButton ToolTipService.ToolTip="Open in a new window">
                                    <FontIcon Glyph="&#xE8A7;" />
                                </HyperlinkButton>
                            </StackPanel>
                        </Expander.Header>
                        <Grid RowSpacing="10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto" />
                                <RowDefinition />
                            </Grid.RowDefinitions>
                            <StackPanel Spacing="10">
                                <StackPanel Orientation="Horizontal">
                                    <ToggleSwitch Margin="0,0,-70,0" IsOn="{Binding ApplyBasicPixelOperation, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                    <CheckBox IsChecked="{Binding ShowOriginalInBasicPixelOperation, Mode=TwoWay}">Show original</CheckBox>
                                </StackPanel>
                                <ComboBox
                                    Header="Operation"
                                    SelectedValue="{Binding SelectedOperation, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                    SelectedValuePath="Content">
                                    <ComboBoxItem>Add</ComboBoxItem>
                                    <ComboBoxItem>Substract</ComboBoxItem>
                                    <ComboBoxItem>Multiply</ComboBoxItem>
                                    <ComboBoxItem>Divide</ComboBoxItem>
                                </ComboBox>
                                <NumberBox
                                    Width="200"
                                    HorizontalAlignment="Left"
                                    Header="Value"
                                    Maximum="255"
                                    Minimum="0"
                                    SmallChange="10"
                                    SpinButtonPlacementMode="Inline"
                                    Value="{Binding OperationValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>
                            <StackPanel
                                Grid.Row="1"
                                HorizontalAlignment="Center"
                                BorderBrush="{ThemeResource AccentFillColorSecondaryBrush}"
                                BorderThickness="1"
                                CornerRadius="1"
                                Orientation="Horizontal"
                                Visibility="{Binding ApplyBasicPixelOperation, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                                <Image
                                    MaxWidth="{Binding ElementName=imgContainer, Path=MaxWidth}"
                                    MaxHeight="{Binding ElementName=imgContainer, Path=MaxHeight}"
                                    Source="{Binding SelectedImage}"
                                    Stretch="Uniform"
                                    Visibility="{Binding ShowOriginalInBasicPixelOperation, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                                <Border
                                    BorderBrush="{ThemeResource AccentFillColorSecondaryBrush}"
                                    BorderThickness="0.5"
                                    Visibility="{Binding ShowOriginalInBasicPixelOperation, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                                <Image
                                    MaxWidth="{Binding ElementName=imgContainer, Path=MaxWidth}"
                                    MaxHeight="{Binding ElementName=imgContainer, Path=MaxHeight}"
                                    Source="{Binding BasicPixelOperationImage}"
                                    Stretch="Uniform" />
                            </StackPanel>
                        </Grid>
                    </Expander>
                </Grid>
                <Grid Visibility="{Binding ShowContent, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                    <Expander
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Stretch"
                        ExpandDirection="Down">
                        <Expander.Header>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Style="{ThemeResource SubtitleTextBlockStyle}">Salt and pepper noise</TextBlock>
                                <HyperlinkButton ToolTipService.ToolTip="Open in a new window">
                                    <FontIcon Glyph="&#xE8A7;" />
                                </HyperlinkButton>
                            </StackPanel>
                        </Expander.Header>
                        <Grid RowSpacing="10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto" />
                                <RowDefinition />
                            </Grid.RowDefinitions>
                            <StackPanel Spacing="10">
                                <StackPanel Orientation="Horizontal">
                                    <ToggleSwitch Margin="0,0,-70,0" IsOn="{Binding ApplySaltAndPepperNoise, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                    <CheckBox IsChecked="{Binding ShowOriginalInSaltAndPepperNoise, Mode=TwoWay}">Show original</CheckBox>
                                </StackPanel>
                                <NumberBox
                                    Width="200"
                                    HorizontalAlignment="Left"
                                    Header="Noise Level (0-50)"
                                    Maximum="50"
                                    Minimum="0"
                                    SmallChange="2"
                                    SpinButtonPlacementMode="Inline"
                                    Value="{Binding SaltAndPepperNoiseLevel, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>
                            <StackPanel
                                Grid.Row="1"
                                HorizontalAlignment="Center"
                                BorderBrush="{ThemeResource AccentFillColorSecondaryBrush}"
                                BorderThickness="1"
                                CornerRadius="1"
                                Orientation="Horizontal"
                                Visibility="{Binding ApplySaltAndPepperNoise, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                                <Image
                                    MaxWidth="{Binding ElementName=imgContainer, Path=MaxWidth}"
                                    MaxHeight="{Binding ElementName=imgContainer, Path=MaxHeight}"
                                    Source="{Binding SelectedImage}"
                                    Stretch="Uniform"
                                    Visibility="{Binding ShowOriginalInSaltAndPepperNoise, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                                <Border
                                    BorderBrush="{ThemeResource AccentFillColorSecondaryBrush}"
                                    BorderThickness="0.5"
                                    Visibility="{Binding ShowOriginalInSaltAndPepperNoise, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                                <Image
                                    MaxWidth="{Binding ElementName=imgContainer, Path=MaxWidth}"
                                    MaxHeight="{Binding ElementName=imgContainer, Path=MaxHeight}"
                                    Source="{Binding SaltAndPepperNoiseImage}"
                                    Stretch="Uniform" />
                            </StackPanel>
                        </Grid>
                    </Expander>
                </Grid>
            </StackPanel>
        </ScrollViewer>
        <Grid
            Grid.RowSpan="2"
            Grid.ColumnSpan="2"
            Canvas.ZIndex="100"
            Visibility="{Binding IsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressRing IsIndeterminate="True" />
                <TextBlock>Loading...</TextBlock>
            </StackPanel>
        </Grid>
    </Grid>-->
</Window>
